# CPA #2 配置模板
# 与 cpa-1 同构，只是承载另一个 OAuth 订阅账号。
# 共享秘钥用独立值，让两个实例在 sub2api 后台对应两个不同上游账号。

host: "0.0.0.0"
port: 8317

api-keys:
  - "REPLACE_WITH_INTERNAL_SHARED_SECRET_2"

auth-dir: "/root/.cli-proxy-api"

commercial-mode: true

remote-management:
  allow-remote: false
  disable-control-panel: true
