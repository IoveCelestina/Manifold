# 编译产物
bin/
dist/
build/
*.exe
*.dll
*.so
*.dylib

# Go
*.test
*.out
vendor/
.gocache/

# Node
node_modules/
.pnpm-store/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# 密钥与环境变量
.env
.env.local
.env.*.local
*.pem
*.key
*.token

# 部署运行时持久化数据（postgres / redis / sub2api / cpa logs）
deploy/data/
deploy/data-staging/

# 备份产物，绝不入库
backups/
*.tar.gz.gpg
deploy/bootstrap.log

# 品牌化配置可能含真实联系方式 / 域名，仅提交 branding.example.json
deploy/branding.json

# IDE
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db

# 本机工具配置（不属于项目）
.claude/

# sub2api 重构工作区（22k+ 文件，且自带 .git），先保持独立不入主仓
sub2api-src/
